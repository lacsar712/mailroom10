# Mailroom · 实物流邮进局清单（IMb）

## 这不是什么

不是即时消息、不是邮件客户端、不是客服工单。处理的是**纸质信件进局**：Intelligent Mail barcode（IMb）与托盘清单。

## 谁在用

第三方预分拣商（presort bureau）。客户交来成批信件后，要按 USPS IMb 编路由、装托盘、生成 induction manifest，再交给承运商扫描 TRA 事件。

## 核心业务

1. 标题必须是 IMb 载荷：20–31 位数字（含路由 ZIP 与序列）。
2. 正文含 `tray=` 托盘号；标签为邮件级别 `first` / `periodical` / `standard`。
3. 同一 IMb 不得重复入库（slug 唯一）。
4. 扫描件（信封图）作为附件写入数据根，路径安全校验。
5. 导出 zip 的 manifest 必须能被读回，防止空包交差。
6. 事件槽记录「拒收非法条码」「托盘关闭」——不是聊天。

## 运行与验收

- 用非法标题 `hello` 创建必须失败；用 22 位数字标题可以成功。
- 首页是进局台操作屏，不是收件箱。
